# tmdb-reminder backend

FastAPI API and APScheduler worker for the TMDB Reminder application.

- API entrypoint: `tmdb-reminder-api` (`tmdb_reminder.main_api`)
- Worker entrypoint: `tmdb-reminder-worker` (`tmdb_reminder.main_worker`)

See the repository root `README.md` for full documentation.
